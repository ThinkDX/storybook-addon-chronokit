// Public surface of the chronokit addon.
// This is the only directory that ships as part of the published addon —
// everything under `src/demo` is example code, not part of the package.
export { type MockDateParameters, mockDateDecorator } from './mockDateDecorator'
